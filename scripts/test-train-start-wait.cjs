const assert=require('node:assert/strict');require('../world.js');require('../city-life.js');const life=createCityLife(AnimalIsland,[{id:'car'}]);life.buses.length=life.cars.length=life.walkers.length=life.commuters.length=0;const positions=life.trains.map(t=>[t.x,t.y]);assert(life.trains.every(t=>t.wait===60&&t.stop));for(let i=0;i<118;i++)life.tick(.5,null,null);life.trains.forEach((t,i)=>{assert.equal(t.x,positions[i][0]);assert.equal(t.y,positions[i][1]);assert.equal(t.wait,1);});for(let i=0;i<10;i++)life.tick(.5,null,null);assert(life.trains.some((t,i)=>Math.hypot(t.x-positions[i][0],t.y-positions[i][1])>1));console.log('PASS all trains stay at named stations for initial 60 seconds and then depart');
