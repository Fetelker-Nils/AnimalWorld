const assert=require('node:assert/strict');require('../world.js');for(const line of AnimalIsland.railLines)for(let i=0;i<line.route.length;i++){const a=line.route[(i+line.route.length-1)%line.route.length],p=line.route[i],b=line.route[(i+1)%line.route.length],u={x:p.x-a.x,y:p.y-a.y},v={x:b.x-p.x,y:b.y-p.y};assert(Number.isFinite(p.x)&&Number.isFinite(p.y),line.id+' invalid point');assert(Math.hypot(u.x,u.y)>.0001,line.id+' duplicate point');assert(!(Math.abs(u.x*v.y-u.y*v.x)<.001&&u.x*v.x+u.y*v.y<0),line.id+' reverses on a dead-end spur');}console.log('PASS all rail loops have continuous nonzero segments without 180-degree dead ends');
